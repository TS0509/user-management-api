# User Management API
A Node.js + Express backend with JWT authentication and PostgreSQL (Supabase).

## Features
- User registration & login (bcrypt)
- JWT authentication
- Role-based access (Admin/User)
- CRUD operations

## Setup
1. npm install
2. Add `.env`
3. node server.js